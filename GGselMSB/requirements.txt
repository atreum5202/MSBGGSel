# GGselV7 — основные зависимости панели
flask>=2.3
requests>=2.31

# Парсер (модуль parser/) — нужны для парсинга ggsel.net
# curl-cffi — имитация TLS fingerprint Chrome, главная защита от Qrator WAF
# beautifulsoup4 + lxml — парсинг HTML карточек товаров
# httpx — уже нужен для content_gen (Gemini API)
curl-cffi>=0.7
beautifulsoup4>=4.12
lxml>=4.9
httpx>=0.27
websockets>=12.0

# Telegram Bot
aiogram>=3.4
python-dotenv>=1.0

# Watchdog / мониторинг
psutil>=5.9

flask-sock
websocket-client
